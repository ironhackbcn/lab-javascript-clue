function divisibleBy(array, divisor){
   array.forEach(function(element){
    var result = element / divisor;
    var test = Number.isInteger(divide);
    var newArray = [];
    if (test === true){
    newArray.push(divide);
    }
  });
  return new
}